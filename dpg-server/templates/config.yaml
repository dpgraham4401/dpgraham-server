apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "dpg-server.fullname" . }}-config
  namespace: {{ .Values.namespace.name }}
  labels:
    {{- include "dpg-server.labels" . | nindent 4 }}
data:
  DPG_DB_HOST: "192.168.1.201"
  DPG_DB_PORT: "5431"
  DPG_DB_NAME: "dpgraham_v1"
