apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "dpg-server.fullname" . }}-tls-cert
  namespace: {{ .Values.namespace.name }}
spec:
  secretName: {{ .Values.ingress.secretName }}
  issuerRef:
    name: le-issuer
    kind: ClusterIssuer
  dnsNames:
    - {{ .Values.ingress.host }}
